<section class="controls">
  <%= link_to "Add Brew", "#create", { class: "btn create", data: { toggle:"modal" }} %>
<!--   <input class="form-control search" placeholder="Search">
 --></section>
<section class="calendar">
  <section class="row">
  <% @calendar.brew_statuses.each do |status| %>
    <div class="col-md-3">
      <section class="calendar-header">
        <%= status.capitalize %>
      </section>
      <section class="calendar-body">
        <% @calendar.brews_with_status(status).each do |brew| %>
          <%= render 'brew', brew: brew, can_edit: true %>
        <% end %>
      </section>
    </div>
  <% end %>
  </section>
</section>